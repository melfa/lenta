<h2>Events</h2>
<% events.forEach(function (event) { %>
  <div>
    Event: <%= event.user %> написал пост <a href="<%= event.url %>"><%= event.title %></a> в <%= event.publicationTime %>
  </div>
<% }) %>